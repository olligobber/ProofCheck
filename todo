fix
	stop fields clearing on error
	invalid line number error?
	make parser error if it didn't consume all input
	filter spaces before parsing
	import/export window
	save/load from storage

add
	symbol replacement
	si(s) vs si
	first order logic
